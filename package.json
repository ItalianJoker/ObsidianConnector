{
  "name": "obsidian-connector",
  "version": "1.0.0",
  "private": true,
  "description": "Super Productivity plugin that links existing projects to existing Obsidian pages (desktop + mobile).",
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.js",
    "build": "node scripts/build.js",
    "zip": "npm run build && node scripts/zip.js",
    "harness": "npm run build && node scripts/serve-harness.js"
  }
}
